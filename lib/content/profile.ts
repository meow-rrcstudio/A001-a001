// lib/content/profile.ts
// 이 사람의 성향을 한 곳에서 꺼냅니다.
//
// ┌─ 왜 지금 늘 null 인가 ────────────────────────────────────────────
// │ 성향을 정하는 온보딩이 아직 없습니다. 그래서 지금은 아무도 성향이
// │ 없고, 고르는 자리들은 전부 그냥 랜덤으로 돕니다 — pickFor 가 겹치는
// │ 수 0 으로 저절로 그렇게 됩니다(lib/content/pick.ts).
// │
// │ 그래도 자리를 미리 만들어 둡니다. 부르는 쪽마다 null 을 손으로
// │ 적어두면, 온보딩이 붙는 날 그 자리들을 다 찾아다녀야 합니다.
// │ 여기 한 줄만 고치면 질문·배열·확정 멘트가 한꺼번에 살아납니다.
// └──────────────────────────────────────────────────────────────────
import type { TraitProfile } from "@/lib/content/traits"

/** 지금 이 사람의 성향. 온보딩 전이거나 건너뛰었으면 null */
export function useTraitProfile(): TraitProfile | null {
  return null
}
